
#let patterns = (
  element: regex(
    "^(\^\d+)?(_\d+)?"
      + "([A-Za-zα-ωΑ-Ω][a-z]?)"
      + "(?:((?:_?\d+)|(?:_\([^()]*(?:\([^()]*\)[^()]*)*\)))|(\^\.?[+-]?\d+[+-]?|\^[+-]?[IV]+[+-]?|\^\.?[+-.]{1}|\.?[+-]{1}\d?))?"
      + "(?:(_?\d+)|(\^\.?[+-]?\d+[+-]?|\^[+-]?[IV]+[+-]?|\^\.?[+-.]{1}|\^\([^)]*\)|\.?[+-]{1}\d?))?"
      + "(\^\^[+-]?(?:[IViv]{1,3}|\d+))?",
  ),

  group: regex(
    "^((?:\([^()]*(?:\([^()]*\)[^()]*)*\))"
      + "|(?:\{[^{}]*(?:\{[^{}]*\}[^{}]*)*\})"
      + "|(?:\[[^\[\]]*(?:\[[^\[\]]*\][^\[\]]*)*\]))"
      + "(?:((?:_?\d+)|(?:_\([^()]*(?:\([^()]*\)[^()]*)*\)))"
      + "|(\^\.?[+-]?\d+[+-]?|\^[+-]?[IV]+[+-]?|\^\.?[+-.]{1}|\^\([^()]*(?:\([^()]*\)[^()]*)*\)|\.?[+-]{1}\d?))?"
      + "(?:((?:_?\d+)|(?:_\([^()]*(?:\([^()]*\)[^()]*)*\)))"
      + "|(\^\.?[+-]?\d+[+-]?|\^[+-]?[IV]+[+-]?|\^\.?[+-.]{1}|\^\([^()]*(?:\([^()]*\)[^()]*)*\)|\.?[+-]{1}\d?))?",
  ),
  reaction-plus: regex("^\s*\+\s*"),
  reaction-arrow: regex(
    "^\s*(<->|↔|<=>>|<=>|⇔|->|→|<-|←|=>|⇒|<=|⇐|-\/>|</-|<<=>)" + "(?:\[([^\[\]]*)\])?" + "(?:\[([^\[\]]*)\])?" + "\s*",
  ),
  math: regex("^\$[^$]*?\$"),
  aggregation: regex("^\((?:s|l|g|aq,oo|aq|cd|cr|fl|lc|vit|a|ads|pol|mon|sln|am)\)"),
  bond: regex("^(?:——|==|\.\.=|=\.\.|~~\.\.|\.\.~~|~\.\.~|\.\.|~~)"),
  bond-content: regex("^(?:–|==|\.\.=|=\.\.|\xA0\xA0\.\.|\.\.\xA0\xA0|\xA0\.\.\xA0|\.\.|\xA0\xA0)"),
  count: regex("^\d+"),
  precipitation: regex("^\s?(\^|v)\s?"),
)

#let element-capture = (
  a: 0,
  z: 1,
  symbol: 2,
  count-a: 3,
  charge-a: 4,
  count-b: 5,
  charge-b: 6,
  oxidation: 7,
)

#let group-capture = (
  content: 0, // brackets included
  count-a: 1,
  charge-a: 2,
  count-b: 3,
  charge-b: 4,
)

#let arrow-capture = (
  symbol: 0,
  top: 1,
  bottom: 2,
)
